/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['arabiaislamia.org'], // Add the domain of the external image source
    },
  };
  
  export default nextConfig;
  